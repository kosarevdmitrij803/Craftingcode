    var body: some View {
        VStack {
            Spacer()
            Text("Hello World !!!")
        }
        .overlayPreferenceValue(BoundsPreferenceKey.self) { preferences in
            GeometryReader { geometry in
